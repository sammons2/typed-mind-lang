# Shortform syntax is for LLMs
MyFirstApp -> App v1.0.0

# ClassFile fusion operator eliminates boilerplate
App #: main.ts <: Application
  => [start, stop]

start :: () => void
  "Starts the application"
  ~> [initialize, listen]
  $< [PORT, NODE_ENV]