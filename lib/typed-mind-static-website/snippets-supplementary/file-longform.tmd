# Supplementary content for file-longform snippet
# The file-longform snippet already has a program
# but needs some missing entities referenced by functions

# Required functions that are called
function handleError {
  signature: "() => void"
  description: "Handles application errors"
}

function save {
  signature: "(entity: any) => any"
  description: "Saves entity to database"
}

function find {
  signature: "(id: string) => any"
  description: "Finds entity by ID"
}